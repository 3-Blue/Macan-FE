From 2517284123c4096018143a3a17a41b67ec4112f3 Mon Sep 17 00:00:00 2001
From: Refactor Bot <refactor@local>
Date: Tue, 25 Aug 2026 06:34:39 +0000
Subject: [PATCH 8/9] fix(seo): complete, locale-aware sitemap with hreflang;
 shared site URL

- app/sitemap.ts listed only 3 English paths with no locale prefixes. It now
  emits every static path and every published industry page, each with
  hreflang alternates for all four locales, using the default locale as the
  canonical URL.
- Add lib/site.ts (siteUrl from NEXT_PUBLIC_SITE_URL) and use it in the
  sitemap, robots and the layout metadataBase/OG url so canonical URLs agree
  and are configurable per environment.
---
 app/[locale]/layout.tsx |  5 ++--
 app/robots.ts           |  7 +++--
 app/sitemap.ts          | 58 +++++++++++++++++++++++++----------------
 lib/site.ts             |  7 +++++
 4 files changed, 49 insertions(+), 28 deletions(-)
 create mode 100644 lib/site.ts

diff --git a/app/[locale]/layout.tsx b/app/[locale]/layout.tsx
index 42431a9..3f691cb 100644
--- a/app/[locale]/layout.tsx
+++ b/app/[locale]/layout.tsx
@@ -8,6 +8,7 @@ import ThemeRegistry from "@/components/ThemeRegistry";
 import { Header } from "@/components/layout/Header";
 import { Footer } from "@/components/layout/Footer";
 import { routing } from "@/i18n/routing";
+import { siteUrl } from "@/lib/site";
 import { PageTransition } from "@/components/motion/PageTransition";
 import "../globals.css";
 import { CookieConsentBanner } from "@/components/legal/CookieConsentBanner";
@@ -23,7 +24,7 @@ const geistMono = localFont({
 });
 
 export const metadata: Metadata = {
-  metadataBase: new URL("https://www.macan-example.com"),
+  metadataBase: new URL(siteUrl),
   title: {
     default: "MACAN",
     template: "%s | MACAN",
@@ -34,7 +35,7 @@ export const metadata: Metadata = {
     title: "MACAN",
     description:
       "MACAN provides engineering, construction, supply, and project management solutions.",
-    url: "https://www.macan-example.com",
+    url: siteUrl,
     siteName: "MACAN",
     locale: "en_US",
     type: "website",
diff --git a/app/robots.ts b/app/robots.ts
index c2ff2e1..1792e9b 100644
--- a/app/robots.ts
+++ b/app/robots.ts
@@ -1,16 +1,15 @@
 import type { MetadataRoute } from "next";
+import { siteUrl } from "@/lib/site";
 
 export const dynamic = "force-static";
 
 export default function robots(): MetadataRoute.Robots {
-  const baseUrl = "https://www.macan-example.com";
-
   return {
     rules: {
       userAgent: "*",
       allow: "/",
       disallow: ["/admin", "/api"],
     },
-    sitemap: `${baseUrl}/sitemap.xml`,
+    sitemap: `${siteUrl}/sitemap.xml`,
   };
-}
\ No newline at end of file
+}
diff --git a/app/sitemap.ts b/app/sitemap.ts
index 695eaf5..600d270 100644
--- a/app/sitemap.ts
+++ b/app/sitemap.ts
@@ -1,28 +1,42 @@
 import type { MetadataRoute } from "next";
+import { routing } from "@/i18n/routing";
+import { getPublishedIndustrySlugs } from "@/lib/content";
+import { siteUrl } from "@/lib/site";
 
 export const dynamic = "force-static";
 
-export default function sitemap(): MetadataRoute.Sitemap {
-  const baseUrl = "https://www.macan-example.com";
+// Locale-agnostic paths that exist for every locale.
+const STATIC_PATHS = [
+  "",
+  "/about",
+  "/services",
+  "/industries",
+  "/contact",
+  "/privacy",
+  "/terms",
+] as const;
 
-  return [
-    {
-      url: baseUrl,
-      lastModified: new Date(),
-      changeFrequency: "weekly",
-      priority: 1,
-    },
-    {
-      url: `${baseUrl}/about`,
-      lastModified: new Date(),
-      changeFrequency: "monthly",
-      priority: 0.8,
-    },
-    {
-      url: `${baseUrl}/services`,
-      lastModified: new Date(),
-      changeFrequency: "monthly",
-      priority: 0.8,
-    },
+export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
+  const industrySlugs = await getPublishedIndustrySlugs();
+  const paths = [
+    ...STATIC_PATHS,
+    ...industrySlugs.map((slug) => `/industries/${slug}`),
   ];
-}
\ No newline at end of file
+
+  const now = new Date();
+
+  return paths.map((path) => {
+    // hreflang alternates: one URL per locale for this path.
+    const languages = Object.fromEntries(
+      routing.locales.map((locale) => [locale, `${siteUrl}/${locale}${path}`]),
+    );
+
+    return {
+      url: `${siteUrl}/${routing.defaultLocale}${path}`,
+      lastModified: now,
+      changeFrequency: path === "" ? "weekly" : "monthly",
+      priority: path === "" ? 1 : 0.7,
+      alternates: { languages },
+    };
+  });
+}
diff --git a/lib/site.ts b/lib/site.ts
new file mode 100644
index 0000000..fa19416
--- /dev/null
+++ b/lib/site.ts
@@ -0,0 +1,7 @@
+/**
+ * Canonical site origin, used for metadata, robots and the sitemap.
+ * Set NEXT_PUBLIC_SITE_URL in the environment (no trailing slash needed).
+ */
+export const siteUrl = (
+  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.macan-example.com"
+).replace(/\/+$/, "");
-- 
2.43.0

